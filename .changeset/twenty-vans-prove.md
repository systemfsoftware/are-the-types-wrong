---
"@systemfsoftware/arethetypeswrong-cli": minor
---

The package now ships a single self-contained `attw` executable and declares no runtime dependencies, so installing the CLI no longer adds its dependency tree to your project. The command, its flags, and its output are unchanged.
